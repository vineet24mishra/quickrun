import ColorConst from "../colors/ColorConst";

export const ToastStyleColored = {
	backgroundColor: ColorConst.THEME_COLOR_BLUE,
	color: "#ffffff",
	fontSize: 16,
	borderRadius: 3, //ios only
	// fontWeight: "bold",
	yOffset: 40 //android only
};

export const ToastStyleStandard = {
	backgroundColor: "#111111",
	color: "#ffffff",
	fontSize: 16,
	borderRadius: 3, //ios only
	// fontWeight: "bold",
	yOffset: 40 //android only
};

export const ToastStyleLight = {
	backgroundColor: "#ffffff",
	color: "#111111",
	fontSize: 16,
	borderRadius: 3, //ios only
	// fontWeight: "bold",
	yOffset: 40 //android only
};

export const ToastStyleSuccess = {
	backgroundColor: "#03a654",
	color: "#ffffff",
	fontSize: 16,
	borderRadius: 3, //ios only
	// fontWeight: "bold",
	yOffset: 40 //android only
};

export const ToastStyleError = {
	backgroundColor: "#f44336",
	color: "#ffffff",
	fontSize: 16,
	borderRadius: 3, //ios only
	// fontWeight: "bold",
	yOffset: 40 //android only
};
